describe('Validate Error for Invalid Profile Image', () => {\n  it('should display an error for unsupported profile image format', () => {\n    cy.login();\n    cy.visit('/pipeline/create');\n    cy.get('#pipelineName').type('Pipeline with Invalid Image');\n    cy.get('#pipelineDescription').type('Testing invalid image upload.');\n    cy.get('#profileImage').attachFile('invalid_file.exe'); // Unsupported file format\n    cy.get('#saveButton').click();\n    cy.contains('Invalid file format. Please upload a supported image file (e.g., PNG, JPG).').should('be.visible');\n  });\n});